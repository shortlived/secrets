#!/usr/bin/env bash
#MISE description="Show version tags on the remote"
set -euo pipefail
echo "Remote tags:"
git ls-remote --tags origin 'refs/tags/v*' 2>/dev/null \
  | sed 's|.*refs/tags/||' | grep -v '\^{}' | sort -rV
